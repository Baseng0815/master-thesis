%!TEX root = ../../thesis.tex

\subsection{cJSON}\label{sec:target-cjson}

% GROUND TRUTH (~/mugiwara/examples/cjson.rs, ~/mugiwara/src/corpus/,
% ~/fuzzyemu/sysroots/resources/cases/cjson/main.c):
%   - guest: persistent-mode harness reading a fixed 16-byte record from stdin,
%     calling cJSON_ParseWithLength once, freeing the tree and looping. It never
%     exits and never drains stdin to EOF, so a run always ends Yielded and the
%     episode ends on the step budget instead.
%   - MDP: SEED_LEN = 16, CORPUS_SIZE = 1, |A| = 256, 64 mutations = 128 steps per
%     episode, 128 parallel environments.
%   - reward: MarginalWithDepth. Coverage LEVELS (log2 bucket of a block's hit count)
%     this execution reached that the corpus union had never reached, plus
%     --depth-reward-weight per level by which it out-parses the episode's deepest
%     execution.
%   - campaign: 15 arms x 4 seeds x 200 iterations, ~21 h per run
%     (\cref{tab:campaign-inventory}). Best input: `[[[[[2,[[[[[1...`, 831 blocks.

% TODO: The headline corpus target, and the one the whole configuration is developed
% on --- state both facts in the opening paragraph, and state that its reward is raw
% coverage rather than something designed around the agent.
%
% The harness, and why its shape is forced rather than chosen: cJSON has no streaming
% API, and a guest parked in a single drain-to-EOF read never returns to the caller
% under emulation, so the harness is a bounded read of a fixed-size record inside an
% infinite loop. Boot once, snapshot the guest parked on that read, restore per
% execution. Say plainly that a target is a program PLUS a harness and that this one
% was written for the thesis; the record length in particular is an experimental
% parameter, not a property of cJSON.
%
% Why coverage carries signal here at all, independent of any learning result: valid
% JSON reaches deep parser branches and malformed input bails out early, and because a
% coverage level is the log2 bucket of a block's HIT COUNT rather than mere presence,
% deeper nesting is itself new coverage. That is the axis the agent's best inputs climb
% --- report the measured separation between structured and unstructured records
% (\cref{fig:structural-rate} gives the chance rate at 0.086) as evidence the signal
% exists before any agent is involved.
%
% Why the reward is marginal-with-depth and not simply coverage. Both halves earn a
% sentence, because both are load-bearing and both are attack surface for a reviewer:
%   - marginal: re-covering known blocks is worth exactly zero. Under a memoryless
%     reward every substitution would be a safe action, and the agent would learn to
%     park on a byte position that provably finds nothing,
%   - depth: marginal novelty alone ranks a discovery by WHEN it happens rather than
%     how far it gets, and a structurally complete document can only be found late ---
%     by then its deep parse walks almost entirely through blocks the union holds.
% Note that W0 (\cref{sec:results-ablations}) disables the depth term precisely so this
% justification is testable rather than asserted.
%
% Its role in the evaluation, which is what earns it the space: this is the only target
% with an ablation. Fifteen arms, four seeds each, every arm exactly one factor away
% from the frozen baseline and the baseline a cell of every factor, so a difference is
% attributable to a single setting. Name the factor families here in one sentence each
% --- what the agent sees (NOCOV, NOSEED), what coverage means (EDGE), what it is paid
% (W0), how it explores (T15/T25, ALPHA004/ALPHA010), how hard it searches
% (SIMS50/SIMS100), how much it trains per sample (REUSE4), and how much capacity it
% has (SMALL/MEDIUM) --- against a structure-blind random control (CTRL-RAND) at the
% same execution budget. Leave every RESULT to \cref{sec:results-ablations}; this
% section says what was varied and why cJSON is the target to vary it on.
%
% Say why the bound is on learner iterations rather than wall-clock: at 128 episode
% completions per iteration and 64 executions per episode every arm gets exactly the
% same 3.3M guest executions whatever its temperature, search width or gradient-step
% count. That equality is what makes the ablation a fuzzing comparison rather than a
% compute comparison, and it is worth one sentence here because it also explains the
% several-fold spread in the wall-clock column of \cref{tab:campaign-inventory}.
%
% OPEN ITEMS to resolve before writing:
%   - REUSE8 is in the campaign matrix but not in \cref{tab:campaign-inventory}. Either
%     it is missing from the extraction or the arm never completed --- find out and
%     either report 16 arms or say why one was dropped.
%   - the arms did not all run on one revision (\texttt{20e667d} vs \texttt{bb4ce89}).
%     That is a real threat to "one factor away from the baseline" and belongs in this
%     chapter as well as in \cref{sec:discussion-threats}.
