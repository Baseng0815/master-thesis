%!TEX root = ../thesis.tex

\chapter{The MOGI Platform}\label{chap:platform}

We implement our fuzzer on top of the MOGI emulation framework\todo{cite mogi}.
MOGI supports CPU and MMU emulation for various architectures and includes
instrumentation support as well as the possibility for emulating Linux userspace
binaries.

\section{Overview}\label{sec:platform-overview}

\todo{generate architecture figure that contain all components and describe them}

\section{Emulation and the Guest Model}\label{sec:platform-emulation}

% TODO: Backends (only Unicorn dispatches the block hooks the coverage signal needs),
% the guest environment, syscall handling, and how input reaches the PUT --- the
% stdin/scheduler path for the byte-fed targets and the socket path for open62541.
% Note the harness style each target uses (run-to-exit vs.\ persistent-mode loop) and
% why it matters for snapshot restore.

\section{Coverage Instrumentation}\label{sec:platform-coverage}

% TODO: BlockHitTracker: a CPU block hook fires on every executed basic block and its
% address is hashed into a fixed bucket array. State the bucket count actually used
% per target, and be explicit about the consequences, since both formulations depend
% on this vector:
%   - hash collisions alias distinct blocks (same aliasing critique as AFL's bitmap,
%     \cref{sec:fuzzing-cgf}),
%   - it is a block, not an edge, signal,
%   - the fixed width bounds the observation size --- a scaling limit to carry into
%     \cref{sec:discussion-threats}.

\section{Snapshots and the Reset Primitive}\label{sec:platform-snapshots}

% TODO: Snapshot capture and restore (build_emulator / setup_emulator, EmulatorArgs),
% what is and is not restored, and the cost of a restore. Make the connection
% explicit: this is exactly the environment reset an episodic MDP assumes
% (\cref{sec:rl-mdp}), and it is what makes per-episode experimentation on a stateful
% target tractable at all.

\section{Determinism}\label{sec:platform-determinism}

% TODO: The evidence that the same input yields an identical block-coverage vector on
% every execution --- no address drift, clean snapshot/restore, no RNG in the guest.
% This is a short section but a load-bearing one: it is what licenses the claim in
% \cref{chap:methodology} that all run-to-run variance is algorithmic rather than
% environmental. Report the check that was actually run.

\section{Throughput}\label{sec:platform-throughput}

% TODO: Executions per second under emulation, and the split between emulation,
% network inference and training. This is the honest counterweight to the
% sample-efficiency argument and feeds the wall-clock currency of
% \cref{sec:methodology-currencies}.
