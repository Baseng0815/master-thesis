%!TEX root = ../../thesis.tex

\section{Input-Level Action Spaces}\label{sec:results-input-actions}

% Answers RQ1 (\cref{sec:introduction-rqs}). Formulation: \cref{sec:input-actions}.

% TODO: Results in order of increasing evidentiary weight, so the reader sees the
% diagnostic results as diagnostics:
%
%   1. Sanity rungs (\cref{sec:target-bandit,sec:target-contextual-bandit}): report
%      briefly, as preconditions.
%   2. Sequence (\cref{sec:target-sequence}): the headline learning result. Solve rate
%      with interval, executions to solve conditional on success, depth curves against
%      the coverage-guided climber and the oracle. Then the mechanism figures --- does
%      the model rank the correct action first at each depth, and hold it? --- because
%      "it works" and "it works for the reason claimed" are separate claims and the
%      probe suite can distinguish them.
%   3. Branches (\cref{sec:target-branches}): does the agent look past the sled and the
%      survival corridor to the deep optimum? This is where planning should show up if
%      it shows up anywhere.
%   4. cJSON and open62541: raw-coverage targets, where the real-world weight sits.
%      Report coverage against executions and, for open62541, whether an activated
%      session is reached and how quickly.
%
% For every claim, name the baseline and the currency (\cref{sec:methodology-currencies}).
% Report failed and collapsed runs here rather than deferring them all to
% \cref{chap:failure-modes}; that chapter explains them, this one counts them.
