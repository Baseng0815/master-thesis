%!TEX root = ../../thesis.tex

\subsection{Action Space}\label{sec:input-action-space}

% TODO: The action set is the input alphabet, so \verb|Action::size()| is the alphabet
% size and constraint C1 of \cref{sec:mdp-constraints} is satisfied trivially --- no
% packing is needed. Cover:
%   - the alphabet is a per-target choice, not a property of the formulation: the full
%     byte range in general, and the 26 letters of sequence
%     (\cref{sec:target-sequence}) as the instance actually evaluated,
%   - why restricting the alphabet is legitimate and what it costs (it is a form of
%     grammar knowledge --- see the generation-based discussion in
%     \cref{sec:fuzzing-input-generation}). Restricting sequence to 26 letters is the
%     worked example, and it is worth conceding plainly that the restriction is what
%     keeps the branching factor tractable,
%   - the branching factor that results, versus the corpus formulation's
%     (\cref{sec:corpus-action-space}), and what that means for the MCTS budget.
